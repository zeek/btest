#! /usr/bin/env bash
#
# A finalizer to diff the output generated by one of the other btest-rst-*
# commands.

# btest-rst-cmd generates this file if run from inside btest (but not Sphinx).
output="btest-${TEST_NAME}.rst"

if [ ! -e "$output" ]; then
    exit 0
fi

exec btest-diff $output
